# DATABASE_URL debe apuntar a tu Postgres local o remoto
# Si usas el docker-compose incluido (solo DB):
# DATABASE_URL=postgres://appuser:supersecret_dev@localhost:5432/appdb
DATABASE_URL=postgres://usuario:password@localhost:5432/mi_db
PORT=3000
